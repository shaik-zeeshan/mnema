[package]
name = "capture-types"
version = "0.1.0"
edition = "2021"

[dependencies]
capture-metadata = { path = "../capture-metadata" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
