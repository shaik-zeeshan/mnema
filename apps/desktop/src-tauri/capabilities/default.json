{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main, onboarding, settings, and debug windows",
  "windows": ["main", "onboarding", "settings", "debug"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-ask",
    "opener:default",
    "os:default",
    "clipboard-manager:allow-write-image",
    "clipboard-manager:allow-write-text",
    {
      "identifier": "fs:allow-write-file",
      "allow": [{ "path": "$DOWNLOAD/*" }]
    }
  ]
}
