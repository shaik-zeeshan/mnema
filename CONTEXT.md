# Mnema Context

The root context file has been split. Start with [CONTEXT-MAP.md](CONTEXT-MAP.md), then open the owner-local `CONTEXT.md` for the app or crate you are changing.

System-wide decisions remain in [docs/adr](docs/adr/). Future context-specific decisions should live under `<context>/docs/adr/` and be linked from the local context file.
