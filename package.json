{
  "name": "mnema",
  "private": true,
  "license": "MIT",
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=desktop",
    "dev:sandbox": "./scripts/dev-app.sh",
    "build": "turbo run build --cache=local:r",
    "check": "turbo run check --cache=local:r",
    "tauri": "turbo run tauri --filter=desktop --",
    "dev:web": "turbo run dev --filter=web",
    "build:web": "turbo run build --filter=web",
    "check:web": "turbo run check --filter=web",
    "build:macos-local-sign": "./scripts/build-macos-local-sign.sh",
    "release": "./scripts/release.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
