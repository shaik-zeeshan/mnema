{
  "name": "z",
  "private": true,
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=desktop",
    "build": "turbo run build --filter=desktop",
    "check": "turbo run check --filter=desktop",
    "tauri": "turbo run tauri --filter=desktop --"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
